import CartPage from '../../components/CartPage/cartPage';
import React, { Fragment } from 'react';

const Cart = () => {
    return (
        <Fragment>
            <CartPage />
        </Fragment>
    );
};

export default Cart;
